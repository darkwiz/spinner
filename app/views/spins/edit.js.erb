var title       =   '.modal-title',
    loader      =   '.ajax-loader',
    content     =   '.modal-body-content',
    dataRemote  =   'a[data-remote=true]';
    toolbar     =   'div.modal-footer';
    modal       =   '#ajax-modal'

$('.modal-title:last-child').html("Edit Spin");
$('div.modal-footer input').val("Commit Changes");

$('#spin_content').val("<%= escape_javascript(@spin.content) %>");

$(modal).modal();