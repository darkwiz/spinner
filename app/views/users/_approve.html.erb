<%= form_for(current_user.reverse_relationships.find_by_follower_id(user_approved), html: { method: :put , class: 'approve-reject'}) do |f| %>
  <%= f.submit "Accept", class: "btn btn-primary" %>
<% end %>